<!DOCTYPE html>
<html>
<head>
    <title>Hotel Management</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"
          integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link href="~/Content/Site.css" rel="stylesheet" type="text/css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>


</head>
<body>
    <div class="navbar navbar-fixed-top">
        <div class="container-box">
            <div class="navbar-header">
                @{ var user = (DomainObjects.Entities.User)Session["CurrentUser"];}
                @{ bool isAuthenticated = user != null;}
                @if (isAuthenticated && user.RoleId == DomainObjects.Enums.RolesEnum.Admin)
                {
                    <a class="btn-menu" href="@Url.Action("Index", "Home")">Homepage</a>
                    <a class="btn-menu" href="@Url.Action("MySurveys","Home")">My surveys</a>
                    <a class="btn-menu" href="@Url.Action("FilledSurveys","Home")">Completed surveys</a>
                    <a class="btn-menu" href="@Url.Action("Logout","Authentication")">Logout: @user.FirstName @user.LastName</a>
                    <a class="btn-menu" href="@Url.Action("MyProfile","Account")">My profile</a>
                    <a class="btn-menu" href="@Url.Action("CreateSurvey","Home")">Create survey</a>
                    <a class="btn-menu" href="@Url.Action("Index","Account")">All users</a>
                    <a class="btn-menu" href="@Url.Action("Followed","Account")">Followed</a>
                    <a class="btn-menu" href="@Url.Action("Following","Account")">Followers</a>
                }
                else if (isAuthenticated)
                {

                }
                else
                {
                    <a class="btn-menu" href="@Url.Action("Login", "Authentication")">Login</a>
                    <a class="btn-menu" href="@Url.Action("NewUser", "Authentication")">New account</a>
                }



            </div>
        </div>
    </div>
    <div class="container body-content">
        @if (TempData["message"] != null)
        {
            <div class="alert alert-success">@TempData["message"]</div>
        }
        <div class="main">
            <div class=" container-main">
                @RenderBody()
            </div>
        </div>
        <br />
        <footer>
            <p>&copy; @DateTime.Now.Day.@DateTime.Now.Month.@DateTime.Now.Year</p>
        </footer>
    </div>


</body>
</html>